apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db2-persistent-volume-claim
  namespace: ${DEMO_NAMESPACE}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: pv0003
